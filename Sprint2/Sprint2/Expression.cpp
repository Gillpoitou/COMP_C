#include "Expression.h"

void ExpressionConst::buildASM(ostream &o, map<string,int>* symbolTable, int* lastOffset){
    

}

void ExpressionVar::buildASM(ostream &o, map<string,int>* symbolTable, int* lastOffset){
    
}